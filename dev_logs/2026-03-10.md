# 2026-03-10

## 本日反馈与修复

### 1) 开局构筑（拖拽选卡）
- 新增构筑卡池 API：`GET /api/player-cards`（仅中文字段）与 `GET /api/deck-presets`。
- 构筑器卡池“空白/拖不进去”问题：
  - 增加加载态与失败提示（提示重启 `frontend/server_core.py` 后刷新）。
  - 增加点击加入（拖拽不可用时仍可构筑）。
  - 拖拽兼容性增强：dataTransfer 同时写入 `text/plain` 与 `text`，drop 端做 fallback。
- 卡牌数量规则：默认每张牌最多 2 张。
  - 前端：点击/拖拽/文本解析/从预组载入都会自动限制到 2 张，并给出提示。
  - 后端：`deck_text` 解析也做 2 张上限兜底，避免绕过前端。
- 选卡时悬浮卡牌详情：增加 tooltip（中文）展示卡名、职业/类型、费用/等级/槽位、中文说明。

### 2) 游戏流程与 UI
- 地点连接缺少提示：
  - 每个地点节点增加“连接：……”列表。
  - 地图下方增加“地点连接边列表（A ↔ B）”，并高亮当前可直达的边。
- 战斗武器选择逻辑依赖英文卡名导致武器弹窗不出现：
  - 改为使用 `card_id + slots + uses (+traits)` 判断是否为武器，不依赖英文名。
  - 同步补充 play_area 下发 `traits`，避免页面侧筛选失败。

### 3) 事件卡效果核对：荒唐的素描像
- 引擎侧效果可触发，但 UI 侧容易“看不出来”。
- 新增 server 日志：打出卡牌后记录手牌/牌库/弃牌变化（便于肉眼确认抽牌是否发生）。

## 回归校验
- `pytest -q backend/tests`：通过（332）。
- `python3 -m py_compile frontend/server_core.py`：通过。
