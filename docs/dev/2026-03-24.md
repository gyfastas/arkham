# 2026-03-24 开发日志

## Bug 修复

### 1. 佐伊·萨马拉斯 - 简体中文统一 & 技能实现

**问题**:
- 佐伊的名字和描述使用繁体中文（薩馬拉斯、主廚）
- 佐伊的技能（与敌人交战获得1资源）未实现
- 专属卡/弱点未在调查员JSON中定义

**修复**:
- `data/investigators/zoey_samaras.json`: 繁体→简体，添加 signature_cards 和 weakness
- `data/player_cards/neutral/zoeys_cross_lv0.json`: 描述繁体→简体
- `data/player_cards/neutral/smite_the_wicked_lv0.json`: 描述繁体→简体
- `backend/cards/guardian/zoey_samaras.py`: 新建技能实现（reaction + elder sign效果）
- `backend/tests/test_cards/test_zoey_samaras.py`: 4个单元测试

**状态**: ✅ 已修复，375 tests passed

---

### 2. 推荐卡组不生效

**问题**:
- 点击"开始游戏"后卡组未正确加载
- 客户端发送 `deckPreset = 'default'`，但服务端预设ID格式为 `{investigator_id}_starter`

**修复**:
- `client/src/views/LobbyView.vue` 第138行: `'default'` → `${store.selectedInvestigator}_starter`

**状态**: ✅ 已修复

---

### 3. 构筑卡组不显示专属卡/弱点

**问题**:
- 佐伊的专属卡（佐伊的十字架）和弱点（击退邪恶）未在构筑界面显示

**修复**:
- `data/investigators/zoey_samaras.json`: 添加 `"signature_cards": ["zoeys_cross_lv0"]`, `"weakness": "smite_the_wicked_lv0"`

**状态**: ✅ 已修复

---

### 4. 进入游戏后行动选项消失

**问题**:
- 只有"结束回合"按钮，没有调查/战斗/抽牌等行动按钮
- `actions_remaining` 默认为0，前端只在 >0 时显示按钮

**根本原因**:
- `GameSession.setup()` 未初始化调查员行动点
- 行动点只在 `handle_end_turn()` 中设置

**修复**:
- `server/game_session.py` 第502-510行: 添加行动点初始化
```python
inv = g.state.get_investigator("player")
if inv:
    inv.actions_remaining = 3
```

**状态**: ✅ 已修复，375 tests passed

---

## 当前服务器状态
- ✅ 运行中: http://localhost:8910
- ✅ 测试通过: 375 tests
